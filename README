1. Scrape CNN transcripts (beautifulsoup)
	a. Create separate documents for shows
2. Submit transcripts to claimbuster API
	a. API returns .txt with score of each sentence, speaker attached to first score
3. store in db
	a. relations (show, person)
	b. (show, sentences)
	c. (speaker, claim, transcript id) (trnas id, trans text, show information)